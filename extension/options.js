const $=id=>document.getElementById(id);chrome.storage.sync.get(['backendUrl'],d=>{$('backend').value=d.backendUrl||'http://localhost:3000'});$('save').onclick=async()=>{const backendUrl=$('backend').value.replace(/\/$/,'');await chrome.storage.sync.set({backendUrl});let msg='Saved extension backend URL.';const body={};if($('google').value)body.googleApiKey=$('google').value;if($('barikoi').value)body.barikoiApiKey=$('barikoi').value;if(Object.keys(body).length){try{const r=await fetch(backendUrl+'/api/settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});msg+='\nBackend API keys saved: '+r.status}catch(e){msg+='\nCould not save backend keys: '+e.message}}$('google').value='';$('barikoi').value='';$('log').textContent=msg};
